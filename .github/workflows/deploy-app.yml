name: Deploy Flask App

on:
    workflow_dispatch:

jobs:
    deploy:
        runs-on: self-hosted

        steps:
        - name: checkout code 
          uses: actions/checkout@v4

        - name: Deploy
          run: docker-compose -f ./docker-compose.yml up -d --build

      



